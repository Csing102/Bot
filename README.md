# Smart Assistant with OpenAI + Flask

This project is a GPT-4-powered assistant that uses tools (like code interpreter), memory, and a simple web interface.

## Setup

1. Install dependencies:
```bash
pip install -r requirements.txt
